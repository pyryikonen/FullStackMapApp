version: "3.8"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      - USER=tfpyik
      - PASSWORD=9p607C36
      - HOST=mydb.tamk.fi
      - PORT=3306
      - DB=dbtfpyik1
    depends_on:
      - nginx
  nginx:
    image: nginx:alpine
    volumes:
      - ./frontend/dist:/usr/share/nginx/html
    ports:
      - "80:80"
